# Architecture

## Concept

The GCB Study Platform is a file-first theological application.

## Core Flow

```text
content/ + data/ -> validated by schemas -> surfaced in app/ -> consumed by readers, students, agents